name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - '.github/workflows/pages.yml'
      - 'package.json'
      - 'tsconfig.json'
      - 'Sources/**'
      - 'README.md'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build
        working_directory: .

      - name: Build LaTeX Technical Paper
        uses: xu-cheng/latex-action@v3
        with:
          root_file: docs/yankovinator.tex
          latexmk_use_xelatex: false
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode
          working_directory: docs

      - name: Build LaTeX Presentation
        uses: xu-cheng/latex-action@v3
        with:
          root_file: docs/presentation.tex
          latexmk_use_xelatex: false
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode
          working_directory: docs

      - name: Build LaTeX Reference Manual
        uses: xu-cheng/latex-action@v3
        with:
          root_file: docs/reference.tex
          latexmk_use_xelatex: false
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode
          working_directory: docs

      - name: Verify PDFs exist
        run: |
          if [ ! -f docs/yankovinator.pdf ]; then
            echo "Error: yankovinator.pdf not found"
            exit 1
          fi
          if [ ! -f docs/presentation.pdf ]; then
            echo "Error: presentation.pdf not found"
            exit 1
          fi
          if [ ! -f docs/reference.pdf ]; then
            echo "Error: reference.pdf not found"
            exit 1
          fi
          echo "✓ PDFs built successfully"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
